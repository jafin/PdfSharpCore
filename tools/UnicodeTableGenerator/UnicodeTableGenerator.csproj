<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>disable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
    <!--
      Deliberately outside PdfSharpCore.slnx. This runs by hand when the Unicode version is
      bumped and has no business in the build or in CI; what it produces is checked in.
    -->
  </PropertyGroup>

</Project>

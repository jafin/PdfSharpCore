name: dotnet package

on:
  # A branch with a pull request open is covered by the pull_request trigger below. Building it
  # here as well would run everything twice for the same commit.
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  # So that a branch with no pull request open can still be built, on request.
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Setup dotnet
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: |
            8.0.x
            10.0.x
      - name: DotNetBuild
        shell: pwsh
        run: ./ci-build.ps1
      # The Ghostscript.NativeAssets package covers Windows, where ImageMagick loads the
      # Ghostscript DLL in process. On Linux it shells out to the system "gs" delegate instead,
      # so the visual comparison tests still need Ghostscript installed here.
      - name: Install Ghostscript
        run: sudo apt-get install -y ghostscript
      - name: Test
        run: dotnet test --no-restore --verbosity normal /p:CollectCoverage=true /p:CoverletOutputFormat=opencover
      # The DOM's value model is generated rather than reflected for, and EnableTrimAnalyzer proves
      # no statically visible reflection is left. It cannot prove there is none at all, nor that the
      # result runs. Publishing native and running it does. The app returns non-zero if any check
      # fails, so this step is the gate.
      - name: AOT smoke test
        run: |
          sudo apt-get install -y clang zlib1g-dev
          dotnet publish MigraDocCore.AotSmokeTest/MigraDocCore.AotSmokeTest.csproj -c Release -r linux-x64
          ./MigraDocCore.AotSmokeTest/bin/Release/net10.0/linux-x64/publish/MigraDocCore.AotSmokeTest
      # v4 replaced the old uploader binary with the Codecov CLI, and with it the rules about
      # tokens: uploading without one now depends on the account having opted its public
      # repositories out of needing a token. This one uploads without a token and has no
      # CODECOV_TOKEN secret, so this step is where coverage would quietly stop. Quietly because
      # fail_ci_if_error defaults to false and is left there on purpose - an upload is a report
      # about the build rather than a verdict on it - so a rejected one shows in this step's log
      # and nowhere else.
      - name: Codecov
        uses: codecov/codecov-action@v7

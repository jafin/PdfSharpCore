namespace PdfSharpCore.Drawing.Layout.enums;

/// <summary>Specifies where text sits within the height of its layout rectangle.</summary>
public enum XVerticalAlignment
{
    /// <summary>Text sits against the top of the rectangle.</summary>
    Top,
    /// <summary>Text is centred in the height of the rectangle.</summary>
    Middle,
    /// <summary>Text sits against the bottom of the rectangle.</summary>
    Bottom,
}
